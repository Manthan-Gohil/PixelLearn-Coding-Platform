[pytest]
asyncio_mode = auto
testpaths = tests
# Add the service root to sys.path so `app` and `scripts` are importable.
pythonpath = .
# Show local variables in tracebacks and display all warnings.
addopts = -v --tb=short -W default
